*,
*::before,
*::after {
  box-sizing: border-box;
}

html, body {
  margin: 0;
  padding: 0;
}

body {
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: radial-gradient(circle at top, #161b3a 0, #050814 50%, #02030a 100%);
  color: #f5f5f7;
}

a {
  color: inherit;
  text-decoration: none;
}

a:hover {
  text-decoration: none;
}

main, .page-container, .section {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 16px 40px;
}

/* ÜST MENÜ */

.top-nav {
  position: sticky;
  top: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 20px;
  background: rgba(4, 8, 24, 0.9);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}

.top-nav .logo {
  font-weight: 700;
  font-size: 16px;
  letter-spacing: 0.03em;
  color: #f7c35f;
}

.top-nav nav a {
  font-size: 13px;
  margin-left: 18px;
  opacity: 0.75;
}

.top-nav nav a:hover,
.top-nav nav a.active {
  opacity: 1;
  color: #ffffff;
}

/* HERO */

.hero {
  max-width: 1100px;
  margin: 24px auto 8px;
  padding: 32px 20px;
  text-align: left;
}

.hero h1 {
  font-size: 26px;
  margin-bottom: 12px;
}

.hero p {
  max-width: 640px;
  font-size: 14px;
  color: #cfd3ff;
}

/* BÖLÜM BAŞLIKLARI */

.section {
  line-height: 1.7;
}

.section h2 {
  font-size: 20px;
  margin-bottom: 8px;
  color: #f7c35f;
}

.section p {
  font-size: 14px;
  color: #e0e3ff;
}

.sub {
  font-size: 13px;
  color: #c7cae6;
}

/* GRID & CARD */

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 16px;
  margin: 24px 0;
}

.card {
  background: radial-gradient(circle at top left, #242a4a 0, #15182b 40%, #090b16 100%);
  border-radius: 14px;
  padding: 18px 18px 16px;
  box-shadow: 0 18px 35px rgba(0, 0, 0, 0.55);
  border: 1px solid rgba(255, 255, 255, 0.05);
}

.card-title,
.card h3 {
  font-size: 15px;
  font-weight: 600;
  margin: 4px 0 6px;
}

.label {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: #9ca4ff;
}

.meta {
  font-size: 12px;
  color: #b3b8e0;
  margin-bottom: 4px;
}

.price {
  font-size: 14px;
  margin: 4px 0 6px;
  color: #f7c35f;
}

.pi-pill {
  display: inline-flex;
  align-items: center;
  padding: 4px 8px;
  border-radius: 999px;
  background: rgba(101, 88, 255, 0.11);
  font-size: 11px;
  color: #d9ddff;
}

.pi-pill .pi-dot {
  display: inline-block;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: #f7c35f;
  margin-right: 6px;
}

.btn-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 10px;
}

/* BUTONLAR */

.btn,
.btn-small {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 8px 14px;
  border-radius: 999px;
  font-size: 13px;
  border: 1px solid transparent;
  cursor: pointer;
  background: #262b50;
  color: #ffffff;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.35);
}

.btn-small {
  font-size: 12px;
  padding: 6px 12px;
}

.btn-primary {
  background: linear-gradient(135deg, #f6b85b, #f7823f);
  color: #05050a;
  border-color: rgba(0, 0, 0, 0.1);
}

.btn-outline {
  background: transparent;
  border-color: #5e65ff;
  color: #e0e4ff;
}

.btn:hover,
.btn-small:hover {
  filter: brightness(1.07);
  transform: translateY(-1px);
}

.cta-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 16px;
}

/* CHIP / FİLTRELER */

.chip-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 16px 0;
}

.chip {
  padding: 6px 12px;
  border-radius: 999px;
  font-size: 12px;
  background: rgba(16, 20, 48,
